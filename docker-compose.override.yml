services:
  server:
    volumes:
      - ./server:/app:delegated
      - ./ml:/ml:delegated
    command: >
      sh -c "uvicorn app:asgi_app --host 0.0.0.0 --port 5000 --reload"
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "5000:5000"
