# --- Backend Framework ---
fastapi
uvicorn[standard]       # 'standard' includes high-speed loopers like uvloop
redis                   # Redis client with async support (redis[asyncio] available)

# --- Database (Prisma) ---
prisma
psycopg2-binary         # Keep this if using PostgreSQL

# --- Real-time & Events ---
python-socketio[asyncio_client]
python-multipart        # REQUIRED for FastAPI to handle form data/logins
python-dotenv

# --- AI & Vision ---
ultralytics             # YOLOv11
numpy
Pillow
opencv-python-headless  # 'headless' is better for servers/Pi (no GUI needed)

# --- Security & Utils ---
passlib[bcrypt]         # REQUIRED for secure password hashing (Prisma/FastAPI)
python-jose[cryptography] # REQUIRED if you use JWT tokens for AGAPAI login
requests